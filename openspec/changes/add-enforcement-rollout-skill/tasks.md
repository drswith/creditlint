## 1. OpenSpec

- [x] 1.1 Add spec coverage for the enforcement rollout skill and README prompt entrypoint.
- [x] 1.2 Document the rollout layers and squash-merge boundary in the change design.

## 2. Skill

- [x] 2.1 Create a repository-local skill under `skills/` for full enforcement rollout guidance.
- [x] 2.2 Explain required operator actions beyond installing `creditlint`, including hooks, CI, PR text checks, rulesets, merge-bot validation, and repository settings.

## 3. README

- [x] 3.1 Add a README section that points users to the skill.
- [x] 3.2 Add a copyable prompt that asks an agent to apply the full enforcement rollout to the current repository.

## 4. Validation

- [x] 4.1 Run OpenSpec validation after the new skill and README updates.
