name: release

on:
  workflow_dispatch:
    inputs:
      publish_crate:
        description: "Publish the crate to crates.io after building artifacts"
        required: false
        default: false
        type: boolean
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    name: validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Install pnpm workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Lint
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo nextest run

      - name: Test npm wrapper
        run: pnpm --filter creditlint test

      - name: Validate OpenSpec
        run: pnpm dlx @fission-ai/openspec validate --all

  build-release-artifacts:
    name: build-${{ matrix.name }}
    needs: validate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x64
            os: ubuntu-latest
            binary_path: target/release/creditlint
            asset_name: creditlint-x86_64-unknown-linux-gnu
            expected_file_pattern: "x86-64"
            use_cross: false
            skip_smoke: false
          - name: linux-arm64
            os: ubuntu-latest
            binary_path: target/aarch64-unknown-linux-gnu/release/creditlint
            asset_name: creditlint-aarch64-unknown-linux-gnu
            expected_file_pattern: "aarch64|ARM aarch64"
            use_cross: true
            target: aarch64-unknown-linux-gnu
            skip_smoke: true
          - name: macos-arm64
            os: macos-15
            binary_path: target/release/creditlint
            asset_name: creditlint-aarch64-apple-darwin
            expected_file_pattern: "arm64"
            use_cross: false
            skip_smoke: false
          - name: macos-x64
            os: macos-15-intel
            binary_path: target/release/creditlint
            asset_name: creditlint-x86_64-apple-darwin
            expected_file_pattern: "x86_64"
            use_cross: false
            skip_smoke: false
          - name: windows-x64
            os: windows-latest
            binary_path: target/release/creditlint.exe
            asset_name: creditlint-x86_64-pc-windows-msvc.exe
            use_cross: false
            skip_smoke: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cross
        if: matrix.use_cross
        uses: taiki-e/install-action@cross

      - name: Build release binary
        if: ${{ !matrix.use_cross }}
        run: cargo build --release

      - name: Build release binary with cross
        if: matrix.use_cross
        run: cross build --release --target ${{ matrix.target }}

      - name: Verify Unix artifact architecture
        if: runner.os != 'Windows'
        run: |
          file "${{ matrix.binary_path }}"
          file "${{ matrix.binary_path }}" | grep -E "${{ matrix.expected_file_pattern }}"

      - name: Smoke test release binary
        if: runner.os != 'Windows' && !matrix.skip_smoke
        run: ./scripts/smoke-release-binary.sh ./target/release/creditlint

      - name: Smoke test release binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $repo = Join-Path $env:RUNNER_TEMP "creditlint-smoke-repo"
          New-Item -ItemType Directory -Force -Path $repo | Out-Null
          Set-Location $repo
          git init | Out-Null
          git config user.name "Creditlint Smoke Test"
          git config user.email "creditlint-smoke@example.com"
          & "$env:GITHUB_WORKSPACE\target\release\creditlint.exe" init | Out-Null
          "Reviewed-by: Jane Doe <jane@example.com>" | & "$env:GITHUB_WORKSPACE\target\release\creditlint.exe" check --stdin | Out-Null
          "Co-authored-by: Codex <codex@example.com>" | & "$env:GITHUB_WORKSPACE\target\release\creditlint.exe" check --stdin *> $null
          if ($LASTEXITCODE -ne 1) {
            throw "expected violating input to exit 1"
          }
          & "$env:GITHUB_WORKSPACE\target\release\creditlint.exe" github ruleset-pattern | Out-Null

      - name: Stage artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp "${{ matrix.binary_path }}" "dist/${{ matrix.asset_name }}"

      - name: Stage artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "${{ matrix.binary_path }}" "dist/${{ matrix.asset_name }}"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.asset_name }}

  checksums:
    name: package-release-assets
    runs-on: ubuntu-latest
    needs: build-release-artifacts
    permissions:
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate SHA256SUMS
        run: |
          cd dist
          sha256sum creditlint-* > SHA256SUMS
          cat SHA256SUMS

      - name: Upload checksum workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: SHA256SUMS
          path: dist/SHA256SUMS

      - name: Publish checksum release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/creditlint-*
            dist/SHA256SUMS

  publish-npm:
    name: publish-npm
    runs-on: ubuntu-latest
    needs:
      - validate
      - build-release-artifacts
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Print npm version
        run: npm --version

      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/release
          merge-multiple: true

      - name: Stage npm native binaries
        run: |
          mkdir -p dist/npm
          cp dist/release/creditlint-aarch64-apple-darwin dist/npm/creditlint-darwin-arm64
          cp dist/release/creditlint-x86_64-apple-darwin dist/npm/creditlint-darwin-x64
          cp dist/release/creditlint-aarch64-unknown-linux-gnu dist/npm/creditlint-linux-arm64
          cp dist/release/creditlint-x86_64-unknown-linux-gnu dist/npm/creditlint-linux-x64
          cp dist/release/creditlint-x86_64-pc-windows-msvc.exe dist/npm/creditlint-windows-x64.exe

      - name: Publish npm packages with trusted publishing
        env:
          NODE_AUTH_TOKEN: ""
        run: scripts/publish-npm-packages.sh --execute --dist-dir dist/npm

  publish-crate:
    name: publish-crate
    runs-on: ubuntu-latest
    needs:
      - validate
      - build-release-artifacts
      - checksums
      - publish-npm
    permissions:
      contents: read
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.publish_crate)
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Verify crates.io token is configured
        run: |
          if [ -z "${CARGO_REGISTRY_TOKEN:-}" ]; then
            echo "CARGO_REGISTRY_TOKEN is not configured" >&2
            exit 1
          fi

      - name: Dry-run crates.io publish
        run: cargo publish --locked --dry-run

      - name: Publish crate to crates.io
        run: cargo publish --locked
